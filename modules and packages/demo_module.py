 #A file containing a set of functions you want to include in your application having .py extension
#suppose I have to build a software "ABCD" with new four features. It is basically combined one project so it is easier if we break it into different modules.
# The main advantages are if we want to change something on a particular module it will not be effect the rests.
# All the same features belonging same module.
# The another advantages are we can reuse the module when we can build another module.
# In one module  there are functions ,variables

def mymodule(name):
    print("This is my :"+name)